import Screen from "../../components/Screen";

import { routes } from "../../router.settings";

const ComicsPageContainer = () => {
  return <Screen title={routes.COMICS.name}>Comics page content</Screen>;
};

export default ComicsPageContainer;
