import Screen from "../../components/Screen";

import { routes } from "../../router.settings";

const EventsPageContainer = () => {
  return <Screen title={routes.EVENTS.name}>Events page content</Screen>;
};

export default EventsPageContainer;
