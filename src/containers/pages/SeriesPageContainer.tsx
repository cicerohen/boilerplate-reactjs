import Screen from "../../components/Screen";

import { routes } from "../../router.settings";

const SeriesPageContainer = () => {
  return <Screen title={routes.SERIES.name}>Series page content</Screen>;
};

export default SeriesPageContainer;
